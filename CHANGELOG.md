# Changelog

## Unreleased

### 默认 Skills

- 右侧默认执行区使用“添加/删除”统一管理；平时仅显示标签，进入管理后显示删除操作，完成后收起。
- 复用现有搜索、分类和技能目录，默认项按任务独立保存；删除只移出默认，不卸载 Skill。
- 已启用并信任摘要提醒时，下一条消息起提醒助手读取默认 Skill；保留手动笔记、输入草稿和摘要其他字段。
- 通用版不预置私人默认项，保留可配置分类、收藏及跨平台提醒命令。

## 2.0.0

对比本仓库公开发布的 **v1.0.1**。

### 工作方式

- 从分散的管理入口转向任务右栏的 **上下文 / Skills / 资产** 三分区。
- 新增独立持久任务摘要，集中记录目标、进展、下一步和约定。
- 新增独立手动笔记、冷历史关联和明确的文件/任务来源引用。
- 新增可选择启用的原生摘要维护提醒；当前助手负责维护，不额外调用总结模型。

### Skills

- 保留已有分类、搜索与收藏；新增右栏快捷使用与原生输入区 Skill 标签。
- 通用版分类、关键词和默认收藏可通过配置修改；不预置作者私人偏好。

### 资产

- 从独立资产控制台入口转向当前项目的历史文件，支持切到公用库。
- 新增原生项目目录解析、多类型文件预览及可记忆的类别排除。
- 公用素材与 MJ 浏览整合；MJ 按完整、原顺序的 P 值组合分组，并可复制 `--p`。

### 安装与扩展

- Windows ZIP 同时包含侧栏与本地资产服务，状态与程序分离。
- 新安装生成随机本机令牌；升级保留配置，卸载保留用户资产。
- 显式的钩子配置/移除入口，不自动改全局规则或信任状态。
- 提供 UI 配置、摘要存储、文件预览、项目解析、MJ 分组等修改入口。
- 运行环境要求 Node.js 22.13+。

**旧版已经有**卡片导航、Skill 分类/收藏、项目素材与生成资产整理；这些不是 2.0 首次出现的能力。新版重点是连接方式、任务接续和项目文件工作流。

## 1.0.1

[原发布记录](https://github.com/q2522879285-source/codex-workspace-enhancer/releases/tag/v1.0.1)
